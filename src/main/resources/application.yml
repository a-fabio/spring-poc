server:
  port: 4000

spring:
  profiles:
    active: h2
  application:
    name: poc

# Configuração do Swagger/OpenAPI
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /api-docs
    enabled: true